syntax = "proto3";

package edgefs;

option cc_generic_services = true;

message ReadRequest {
  string path = 1;
  uint64 offset = 2;
  uint64 len = 3;
}

message ReadReply {
  bytes data = 1;
  uint64 len = 2;
}

service CenterService {
  rpc Read (ReadRequest) returns (ReadReply);
}